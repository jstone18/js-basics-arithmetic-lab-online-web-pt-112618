// Write your code here
const var1 = 31;
const var2 = 2;

const multiply = var1 * var2;

const num1 = Math.ceil(1)
const num2 = Math.floor(2)
const random = Math.ceil(Math.random() * (num2 - num1)) + num1;

const mod = 100 % 6;

const max = Math.max(5, 10, 15, 20)
